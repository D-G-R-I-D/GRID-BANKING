name: Security

on:
  pull_request:
    branches: [dev, main]
  schedule:
    - cron: "0 6 * * 1" # weekly, Monday 06:00 UTC

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --package-lock-only --ignore-scripts --no-audit --no-fund
      # Only production dependencies matter for the deployed app. Dev-tool
      # advisories (prisma CLI, vitest/esbuild) are tracked by Dependabot.
      - run: npm audit --omit=dev --audit-level=high
